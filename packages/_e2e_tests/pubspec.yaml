name: _e2e_tests
description: Internal cross-format integration tests. Not published.
publish_to: none
version: 0.0.0

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  dart_quill_delta: ^10.8.0
  quill_delta_core:
    path: ../quill_delta_core
  quill_delta_docx:
    path: ../quill_delta_docx
  quill_delta_html:
    path: ../quill_delta_html
  quill_delta_markdown:
    path: ../quill_delta_markdown
  quill_delta_pdf:
    path: ../quill_delta_pdf

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
